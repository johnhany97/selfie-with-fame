import React from 'react';

/**
 * @Params
 * none 
 * 
 * @summary
 * Displays H1 tag to notify user that the story has been successfully created
 * 
 * @returns
 * Returns JSX for H1 tag
 */
const Success = () => (<h1>Successfully created story!</h1>);

export default Success;
